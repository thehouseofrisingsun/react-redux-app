import React from 'react'

export default class Dashboard extends React.Component {
    render() {
        let latestEvents = () => {

        }

        let expiringEvents = () => {

        }

        return (
            <div>Latest added events, Expiring events</div>
        );
    }
}